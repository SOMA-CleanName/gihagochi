/// F-012 / F-013 subscription — 라우트 정의.
///
/// 본 슬라이스는 독립 화면이 없어 빈 리스트 export. 일관성 위해 등록은 유지.
library;

import 'package:go_router/go_router.dart';

final List<RouteBase> subscriptionRoutes = <RouteBase>[];
